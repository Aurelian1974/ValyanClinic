@page "/"
@inject NavigationManager Navigation
@rendermode InteractiveServer

@code {
    protected override void OnInitialized()
    {
        // Simplu: la pornire mergem direct la login
        Navigation.NavigateTo("/login", forceLoad: true);
    }
}